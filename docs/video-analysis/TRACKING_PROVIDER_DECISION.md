# FS Player Tracking Provider Decision

## Decision

Tracking Intelligence v2 uses a local, replaceable five-stage pipeline:

1. detection for player, ball, and referee observations
2. segmentation and selected-object propagation
3. multi-object association
4. player re-identification
5. team and shirt classification

No candidate is activated by installation alone. The provider contract requires a pinned source commit, source, model, and packaged-runtime SHA-256 hashes, explicit code and model licences, model-card and training-dataset provenance, bounded input duration, wall time, memory, output size, and concurrency, inference with no network access, and capability-specific real-match benchmark evidence. Re-identification and shirt-number providers need a separate reviewed identity-use decision for every declared training dataset. Readiness also requires the original metadata-only report and a reproducible provider-evidence artifact bound to the exact source, models, provenance, runtime limits, capabilities, thresholds, and report hash.

A single stage provider may contain more than one exact model artifact, which is the preferred boundary for a football detector with separate player, small-ball, and referee heads. Every model in a multi-model provider must explicitly bind its `detect:*` capabilities, every declared provider capability must be covered, and a model cannot claim a capability outside the provider manifest. Existing one-model providers keep their implicit all-capability binding. This preserves one sealed runtime, request, result, and execution fingerprint while making player, ball, and referee model lineage independently auditable; separate pipeline providers are unnecessary unless they also require different runtimes or execution policies.

SAM 2.1 remains the installed, approved selected-object propagation engine and safe manual-prompt fallback. Provider `1.3.0` can propagate 2-8 analyst-selected objects in one shared video state when every prompt uses the same frame and synchronized range. It keeps the verified model resident across sequential bounded jobs, while every job creates and resets its own video state. Cancellation, timeout, protocol drift, inconsistent runtime identity, or invalid stage telemetry terminates that worker generation before another job can run. It still returns separate review tracks and is not treated as an automatic detector or a re-identification engine.

FS Player exposes that distinction in a fail-honest tracking-readiness matrix. The installed SAM provider can make only the selected-object row available; it can never promote player/ball/referee detection, association/re-identification, or team/shirt classification. Partial future provider sets remain visibly incomplete, and full-scene readiness appears only when every required capability is installed. Match evidence and TrackEval readiness remain separate statuses rather than being inferred from installation.

Future stage providers enter the companion through `football-science-tracking-provider-registry-v1`, a read-only local trust boundary. Each installation has one strict `football-science-tracking-provider-installation-v1` marker that binds the canonical provider manifest, original benchmark report, reproducible evidence artifact, sealed runtime artifact, and exact model files by path, byte size, and SHA-256. The registry rejects path escape, symbolic links, duplicate provider identities, hidden manifest fields, changed runtime/model bytes, and a locally rehashed report that no longer reproduces its evidence. It exposes only bounded capability metadata and reason codes to FS Player; local paths, source URLs, model records, reports, and evidence remain private. Registration proves readiness metadata only and never executes provider code.

Execution is a second independent gate. `football-science-tracking-stage-execution-v1` privately resolves the exact verified installation, accepts only one sealed native executable, and on macOS runs it through a default-deny sandbox with network denied. Runtime, model, and source inputs must be regular non-link files, are write-sealed, and bind SHA-256, size, inode, device, mode, modification time, and change time before execution. The same descriptor identity is checked after the process exits and before output is accepted, so a provider or match-source change invalidates the result even if the child reports success. The child receives a scrubbed environment and only the verified provider/model tree, exact session-owned match source, system runtime files, and a private job directory. Output size, wall time, process-group resident memory, provider concurrency, cancellation, provider fingerprint, stage inputs, and the final `football-science-tracking-stage-result-v1` artifact are enforced independently. The public registry reports `executionAvailable` only after both registry and sandbox preflights pass. A provider that is evidence-ready but lacks this executable boundary remains visibly `activation pending`.

Candidate evidence has a deliberately separate path to avoid an approval cycle. `football-science-tracking-candidate-registry-v1` accepts only canonical manifests whose approval state is `candidate`, whose inference network is disabled, and whose licence, training data, dataset rights, runtime, and model hashes are reviewable. It omits benchmark report/evidence because those are the artifacts the candidate run must produce. The same sandbox executor is used in `benchmark-candidate` mode, but the companion exposes a distinct route and marks the registry, job, and client result `benchmarkOnly`. The activated parser still rejects that manifest. Moving a candidate into the approved registry requires a new exact installation marker containing the passed report and reproducible evidence; files are not promoted by changing a status flag in place.

Both candidate and activated readiness now apply a fail-closed product-licence policy in addition to the manifest's human review flag. Known AGPL, GPL, SSPL, and Hippocratic licence identifiers remain blocked with explicit reason codes. A separately acquired commercial/product licence must be represented as a different reviewed licence contract; setting `licenseReviewed` to true cannot override a known restrictive upstream or model boundary.

The candidate registry now has one official local installer rather than relying on hand-built directory layouts. It takes a canonical manifest and exact local artifacts, requires explicit licence acceptance, rejects symbolic links and model-set drift, copies through stable open descriptors, checks source identity before and after every copy, seals runtime/models read-only, and performs an atomic no-overwrite install under a per-registry lock. The installation is removed if registry resolution or native sandbox preflight fails. `tracking:candidate:plan`, `tracking:candidate:install`, and `tracking:candidate:preflight` deliberately have no network downloader and never turn a candidate into an approved provider.

Candidate and approved registries may retain several sealed versions of one provider id so negative controls and reproducible historical runs do not disable the current build. Every executable lookup still requires the exact id and version; an id-only lookup with several versions remains ambiguous, and two directories claiming the same exact id/version are both blocked. At equal declared priority, the benchmark pipeline selects the newest natural release version while preferring a final release over its prerelease. Candidate preflight reports sandbox-ready entries as executable only in `benchmark-only` mode; this never makes them available to activated tracking.

Approved stage activation now has one separate atomic installer rather than a manual marker procedure. `tracking:provider:plan` and `tracking:provider:install` resolve one exact verified candidate installation, read one passed metadata-only report and its reproducible provider evidence, and deterministically derive a new canonical `approved-local-optional` manifest. The candidate directory is never edited, moved, or relabelled. Runtime and model bytes are copied through stable descriptors into the distinct approved registry with the manifest, report, evidence, and installation marker sealed read-only. Candidate and approved artifact/execution fingerprints must remain identical; report/evidence readiness, registry resolution, and native sandbox preflight must all pass before the target directory is retained. Registry overlap, existing targets, ambiguous identities, evidence drift, licence refusal, missing explicit local-activation consent, and sandbox failure stop fail-closed and remove the staged installation. This tool makes a passed decision executable; it cannot make a failed or incomplete candidate pass.

The first reproducible installed full-scene reference is `yolox-s-coco-reference@0.1.2`, still benchmark-only. Its provider source is the attributed Apache-2.0 YOLOX ONNX adapter at commit `709165705bf30b163822e73a2a7ff2480e3d2bc4`; its COCO YOLOX-S model SHA-256 is `c5c2d13e59ae883e6af3b45daea64af4833a4951c92d116ec270d9ddbe998063`. The packaged arm64 runtime is exactly 52,023,920 bytes with SHA-256 `3e0caa99b0e61717f8d79d8b35a1ab24ec9dbbd706b38ec418ee51769ad0b1b7`. Standard macOS PyInstaller one-file runtimes use an unfilterable SysV semaphore between their parent and child process, so FS Player does not widen the provider sandbox to permit it. The reproducible build instead accepts only PyInstaller `6.15.0` sdist SHA-256 `a48fc4644ee4aa2aa2a35e7b51f496f8fbd7eecf6a2150646bbf1613ad07bc2d`, applies the reviewed no-SysV patch SHA-256 `14ec28aebf1e7ae4318dcdb1ebfa5c384210e9cd48d20c67e3b9d35fb1523b34`, fixes Python hash seed and archive time, and rejects a resulting executable unless its architecture, imported symbols, size, and SHA-256 all match the canonical runtime. This trades PyInstaller's narrow signal-forwarding startup synchronization for preserving FS Player's stronger IPC boundary; the outer executor still owns process-group cancellation, timeout, and memory termination.

The first canonical `0.1.2` sandbox run covered five seconds of the private attacking-third case at five sampled frames per second. It produced 416 player observations over 25 sampled frames, no ball observations, emitted no stdout/stderr, and took 18.064 seconds on CPU, a `3.6128x` real-time factor. This proves packaging, model inference, request/result binding, exact input seals, and network-denied execution. It also fails the non-overridable `1.0x` workstation policy and provides no ball-quality evidence, so it cannot approve either declared capability yet.

The 2026-08-31 five-case screening extended that probe across attacking-third, set-piece-box, compact-midfield, fast-transition, and late-open-play footage. Exactly 25 seconds of video produced 1,774 player observations and 18 ball observations across 125 sampled frames. Ball observations appeared in only three cases. The five sandboxed jobs required 90.555 seconds in total; per-case real-time factors ranged from `3.4974x` to `4.0228x`. The immutable metadata-only screening manifest has SHA-256 `132d95db1cc6e278b5e03134b82719f02c32e6701b63bd3357692d61ebd1ed70`, retains exact raw-evidence checksums for every case, and records `failed-screening`, `approvalReady: false`, and `groundTruthEvaluated: false`. Its review gate permits only a separate suggestion layer, forbids ground-truth mutation and locking, requires exhaustive human review, and blocks provider approval. Observation counts and confidence distributions are workload diagnostics only. They do not establish precision, recall, identity continuity, team classification, or football quality. Because the reference fails the workstation policy before ground-truth review, it remains an engineering baseline and must not prelabel official ground truth or enter the normal tracking registry.

The first acceleration audit keeps the exact YOLOX source and model but wraps ONNX Runtime `1.19.2` with its local CoreML execution provider. CoreML accepted 276 of the model's 277 nodes. An unsandboxed five-run tensor probe compiled in 1.959 seconds and then averaged 21.2 ms per warm inference. The default-deny probe initially proved that Apple's compiler creates a temporary sibling beside `TMPDIR`; moving `TMPDIR` to a private subdirectory inside the existing job root kept that sibling inside the already allowed write boundary. With no broader network, provider, or external file access, the same sandbox then compiled in 0.434 seconds and completed its first inference in 87.2 ms. The stage executor now creates that private `0700` temp subdirectory for every provider. Two clean no-SysV PyInstaller builds produced the same 52,022,320-byte arm64 runtime with SHA-256 `b3f0fe475d453269954db034cca1ca0881e29849cd0d35b55db0d33dcf272224`. This proves reproducible packaging and a viable security boundary only; the CoreML candidate remains uninstalled and has no real-match screening or quality approval yet.

The native acceleration candidate `yolox-s-coco-coreml-native-reference@0.3.1` removes the PyInstaller startup path without widening that sandbox. Its source is bound to Football Science commit `90fa827318202b46082030c4c4bb1ea52f2cb36e` and build-script SHA-256 `c7f9a94d5f8659f0da368bcb9eb9eca45de7812d02f58a9a32a3cc6a603e9079`. The build accepts only ONNX Runtime `1.19.2` archive SHA-256 `370c49770e2e1f243e17c7b227bb7f4b3da793b847d02f38016dc0e46c30fbe1`, embedded CoreML dylib SHA-256 `183dca3132c8f0e2f0a1a30da3bcf7dee634cd6a7d5bf99b47f3d7ffe2791799`, FFmpeg `9.0.1` source archive SHA-256 `cf38e0e28c7e5605942c4a77755349b0145804a397af37eb1fb4c77cb237f635`, and detached-signature file SHA-256 `b613a00005232a1245ace7080088781ac23a916119d3e5b0d6c042368eee0177`. The signature file is retained and checksum-bound provenance; the current builder does not yet perform public-key signature verification and must not claim that it does. FFmpeg is rebuilt offline for arm64 macOS 13 with network, programs, shared libraries, hardware decoding, AudioToolbox, VideoToolbox, AVFilter, AVDevice, and SWResample disabled. The generated component allowlist contains only the H.264 decoder/parser, MOV demuxer, and local file protocol. Two clean builds produced the same 29,135,648-byte executable with SHA-256 `1af02eaa25f043b90b14d2c24afadd19888445d1c705635db8a9e209fd0d84e3`; runtime verification also requires `LC_UUID`, macOS 13 minimum deployment, no dynamic ONNX/FFmpeg dependency, and no imported network primitive.

The installed `0.3.1` candidate remains benchmark-only and passed the unchanged `darwin-sandbox-exec-network-denied-v1` boundary without the CoreMedia decoder or IOSurface permissions that the rejected AVFoundation prototype required. Its five-case 2026-08-31 screen processed 25 seconds of match video in 14.855 seconds. Every case remained below real time; the worst real-time factor was `0.7204x`. Across 125 sampled frames it produced 1,755 player and 31 ball observations, with 410/406/87/446/437 total observations in the attacking-third, set-piece-box, compact-midfield, fast-transition, and late-open-play cases. The sealed screening manifest has SHA-256 `cba1db525a4d0e2636e7467078628ddc89f4ac4347e155a342d853cde38ad70c` and independently reverified all five raw-evidence files. This is a workstation and declared-capability screening pass, not provider approval: the annotation pack is still `not-reviewed`, precision/recall and identity continuity are unmeasured, ball coverage remains uneven, and this provider does not declare referee detection, association, re-identification, team, or shirt-number capability.

`yolox-s-coco-coreml-native-reference@0.4.0` corrects that candidate's semantic boundary: the generic COCO person class now declares `detect:person` and emits `person`, never the football-specific `player` role. Two clean arm64 macOS 13 builds were byte-identical at 29,135,648 bytes. The reviewed binary lock is SHA-256 `00683295a76dfc720092c2457143812e41aca32047f3115f04df78bd5926306f`; the builder rejects every other runtime hash or size. Its five-case Match 11 screen processed 25 seconds in 15,311 ms with a worst real-time factor of `0.727x`, producing exactly 1,755 `person`, zero `player`, 31 `ball`, and zero `referee` observations. The independently verified screening manifest is SHA-256 `edb490ebb251a4f6486f947befb7a048000f8b5e9ab19cc6f4da3cd9421cd71a` and records `screening-pass-awaiting-ground-truth`. This proves the role-neutral contract and workstation boundary only. Role accuracy, detection accuracy, ball quality, referee separation, and provider approval remain blocked until the full-scene annotations are exhaustively reviewed and attested.

The complete ten-minute `0.3.1` screen on 2026-08-31 also passed its workstation boundary. It processed 600,000 ms over 3,000 sampled frames in 340,980 ms, with every case below real time and a worst real-time factor of `0.90085x`. The sealed bundle reproduces with SHA-256 `198223973d4e232c945350cb1fc197a65f4cb4cf9b0b516de7570216ec82ee2b` and contains 43,978 player, 355 ball, and zero referee observations. The full-range ByteTrack `0.1.4` follow-up exited non-zero before publishing any association bundle. Its native boundary allows at most 1,024 trajectories per stage result, so the current candidate has not demonstrated that it can preserve acceptable continuity across one complete two-minute case; no chunked result is substituted for that failed evidence. A detection-only preannotation workspace was instead created from the verified full screen, preserving all 44,333 observations as explicit unassociated suggestions. It independently reproduces with SHA-256 `4142598f05a6fd665fcff97a0ec74051789eaca66c6eac0aaaf0236ce9590004` and remains blocked on referee search, exhaustive review, continuity, identity/team assignment, rights attestation, and all quality metrics.

The association candidate `bytetrack-native-reference@0.1.4` uses the official ByteTrack C++ core at commit `d1bf0191adff59bc8fcfeaa0b33d3d1642552a99`, source archive SHA-256 `1bb85ad045049de1fe8d672b4c9700cb1f7ab59bb87084f3550dd9c8c97de6bc`, and Eigen `3.4.0` archive SHA-256 `8586084f71f9bde545ee7fa6d00288b264a2b7ac3607b974e54d13e7162c1c72`. That adapter removes OpenCV from the association core, runs a separate tracker for player, ball, and referee observations, and preserved every unconfirmed detection as a one-observation trajectory. Canonical output uses the official Ryu implementation at commit `4c0618b0e44f7ef027ebae05d2cc7812048f7c8f`, archive SHA-256 `092d0469a28b9854222a6182190816c3b2e04fa291622f97e210cc5e70397d5b`, with its Apache-2.0 licence checksum-pinned. This was necessary because Foundation's JSON number parser does not preserve the same IEEE-754 value for every decimal emitted by Node. The host binds association invocations to the already canonical request SHA-256; the native runtime still validates the complete bounded observation structure and can only echo that sealed host binding in its result. Two clean arm64 macOS 13 builds produced the same 228,352-byte executable with SHA-256 `e0322d560d577dce198e5e54f6449d08c373b65dae9ede37fb008f3ab65b019a`.

The 2026-08-31 `0.1.4` Real Match association screen consumed the exact five sealed `0.3.1` detection evidence files. It preserved lineage for all 1,786 observations, including all 31 ball observations, produced 1,019 trajectories, and remained below real time in every case with a worst real-time factor of `0.075x`. The bundle independently reverified with SHA-256 `d4559c17531ac94443821060c16d441a8c48c436a83610c5073be3b48b98b740`. It is deliberately only `screening-pass-awaiting-ground-truth`: 938 trajectories are singletons, referee input is absent, and reviewed identity continuity, ID switches, HOTA/IDF1, ball association accuracy, team classification, and shirt-number accuracy remain unmeasured. Singleton preservation proves lossless lineage, not tracking quality. Neither this screen nor its technical pass may activate the provider, mutate ground truth, or claim elite identity performance.

`bytetrack-native-reference@0.1.5` removes those synthetic singleton trajectories and leaves each unconfirmed detection unassigned for the existing review lineage layer to preserve. Two clean arm64 macOS 13 builds were byte-identical at 228,352 bytes with SHA-256 `bc6f0dd9351e272feab80d24c915d30d7bee91cae3539c60d4cbdb193ba1fb4b`. The complete ten-minute screen no longer crashes at the 1,024-trajectory guard and independently reproduces with SHA-256 `4bde398d80d25fd85c3a6fc7642b248cc0f3d72c2c73547d60627e35bb0e5e8f`. It processed all 44,333 observations in 1,274 ms, associated 25,350 of them into 1,383 player trajectories, and left 18,983 visible for review. The result is correctly `failed-screening`: assignment coverage is only `0.571809`, no ball or referee trajectory was produced, and identity quality remains unmeasured. Its sealed review gate permits suggestions only. The resulting read-only workspace contains 20,366 review tracks, preserves all 44,333 observations, and independently reproduces with SHA-256 `2f7b886d1bd3563ee709be8a26d8cafe74caa0977342a000f30b2fdd2f48ccb6`; it is not provider approval or ground truth.

`bytetrack-native-reference@0.2.0` extends the exact same association semantics to role-neutral `person` observations. Two clean arm64 macOS 13 builds were byte-identical at 228,352 bytes. The reviewed binary lock is SHA-256 `c777ca4ce1d65b9f0ef149d9ae1be01415ec39b0acfd95345a59248e820c49e2`; the builder rejects every other runtime hash or size. Its verified five-case Match 11 screen consumed all 1,786 observations from the exact `0.4.0` detection bundle, associated 855 observations into 88 `person` trajectories, left every unassigned observation visible to the review layer, and remained below real time with a worst factor of `0.041x`. The sealed manifest is SHA-256 `cd395cd5b65dc3595b39c5e1252faa6ad14ae05db86a680fc44875abf13c1670` and correctly records `failed-screening`: assignment coverage is only `0.478723`, seven trajectories are singletons, 12 discontinuities are reported, and no ball trajectory is produced. The binary identity and role-neutral lineage are approved locks; association quality and provider activation are not approved. Entity-specific association thresholds and full reviewed TrackEval evidence are required before this candidate can claim elite continuity.

`bytetrack-native-reference@0.3.0` adds explicit entity profiles without synthesizing singleton tracks. Person/player/referee association uses a `0.20` tracking threshold, `0.35` start threshold, the official `0.80` primary match threshold, and a 30-frame buffer scaled to the sampled frame rate. Ball association uses `0.02`, `0.05`, and `0.95` thresholds with a shorter 12-frame buffer so low-confidence small-object evidence can be confirmed without retaining a long-lived false ball. The five matching limits are constructor-bound in the patched upstream core rather than hidden score rewriting. Three clean arm64 macOS 13 builds were byte-identical at 228,352 bytes; the user-reviewed binary lock is SHA-256 `bcf4db667f95aec73cdb79e2b085493ce8ed5bb399d028eabf4ee7c05dcf9df4`. On the exact same five `0.4.0` Match 11 inputs it associated 1,333 of 1,786 observations, improving assignment coverage from `0.478723` to `0.746361`, and produced three ball trajectories instead of zero. Every case remained below real time with a worst factor of `0.0856x`. The independently verified screening manifest is SHA-256 `31cb1fe5100402b333c9f294943c534a76a473d6916b0f9bf2c2daf80792cd2e`. It remains correctly `failed-screening`: 453 observations are unassigned, the 125 trajectories include seven singletons and 29 discontinuities, and reviewed identity/ball accuracy does not yet exist. Camera-motion compensation and exhaustive TrackEval ground truth remain required before association approval.

`bytetrack-native-reference@0.4.0` adds deterministic camera-translation compensation to that entity-aware baseline. For each sufficiently populated adjacent sampled-frame pair, the adapter finds mutual nearest person/player/referee observations, rejects pairs beyond `0.15` normalized image units, estimates median center displacement, removes residual outliers inside a bounded `0.008`-`0.06` gate, and accepts at most `0.08` normalized motion. Only ByteTrack's internal association coordinates are stabilized; original observations, boxes, entity labels, confidence, and lineage remain unchanged. Four arm64 macOS 13 builds were byte-identical at 228,384 bytes; the user-reviewed binary lock is SHA-256 `db84c68916bf6b22b9b671d87eb59b70e5ae7e17fe8811055321f8d57a26c690`. On the exact same five Match 11 inputs, it associated 1,571 of 1,786 observations and raised assignment coverage from `0.746361` to `0.879619`. Trajectories decreased from 125 to 111, singleton trajectories from seven to one, and discontinuities from 29 to 23 while preserving three ball trajectories. Every case remained below real time with a worst factor of `0.049x`. The independently verified screening manifest is SHA-256 `52e92b5619672c5b29b414dfc768716d7f8720f0c4467e1c5c16c56dd9a67ef7`. The candidate remains correctly `failed-screening`: 215 observations are still unassigned, no exhaustive human ground truth or ID-switch measurement exists, and the lower fragment count alone does not prove identity accuracy. It remains benchmark-only and cannot be approved until representative full-scene annotations and independent TrackEval continuity metrics pass.

The full-length Match 11 follow-up uses five complete two-minute cases rather than extrapolating the short screen. Role-neutral YOLOX `0.4.0` processed all 600,000 ms below real time, emitted 43,978 `person` and 355 `ball` observations, and independently reproduced detection manifest SHA-256 `3b14f65479533e7b4276bfa99f89a7a16113f27be83e1991fbbef2d1b3b2cf5f`. ByteTrack `0.4.0` associated 40,036 of 44,333 observations (`0.903074` coverage) into 1,204 trajectories, including 26 ball trajectories, with two singletons and 1,090 discontinuities; its verified full-screen manifest is SHA-256 `bdb04d3bc3d1ac34770617c962088c851254cf827010f60d90df853b24ad5ace`. This remains correctly `failed-screening`: 4,297 observations are unassigned, identity continuity is unmeasured, and generic people are not silently promoted to football roles. The self-contained review-only preannotation workspace contains 5,501 suggestions and reproduces as SHA-256 `813e49c582a1300600ca85007049f2d68ee22d92b15e5740482f7b685d9509a3`. Review Studio matches one workspace folder to the connected case by exact source SHA-256, prioritizes every generic `person` as an unresolved role, and blocks ground-truth preparation until all saved people are explicitly classified as player or referee. Its five visible stages reuse the existing immutable workspace, correction, scene-checkpoint, reference-lock, and TrackEval boundaries; the UI adds no parallel source of truth and grants no provider approval.

The re-identification screen now has the same reproducible boundary instead of accepting an informal model demo. `football-science-tracking-reidentification-candidate-screening-v1` consumes only a fully verified detection and association bundle, rebuilds the complete player trajectories from the sealed association request/result, and requires the re-identification request to hash to those exact trajectories. Each immutable case records assignment coverage, identity-cluster and singleton counts, merged trajectory count, mean confidence, temporal identity collisions, wall time, isolation, and real-time factor. The independent verifier rereads sealed raw evidence and reproduces the manifest checksum. A technical screen always remains `approvalReady: false`; HOTA, IDF1, ID switches, cluster accuracy, and analyst correction burden still require reviewed full-scene ground truth and TrackEval. The stage result validator also rejects one opaque identity key assigned to simultaneous player trajectories before the result can reach the review layer.

Full-range detection evidence can now feed a separate `football-science-tracking-candidate-preannotation-workspace-v1`; verified association evidence is optional and accepted only when it covers the same complete cases. The generator refuses partial inputs, converts exact normalized boxes to deterministic zero-based MOT suggestions, retains unassociated observations as visible review work, and writes only a new read-only workspace with per-case track maps. If association is absent or failed, every detection remains explicitly unassociated and the review gate records that work rather than synthesizing continuity. Its independent verifier reproduces every byte from the sealed upstream bundles. The workspace cannot mutate the annotation pack, cannot assert rights or exhaustive review, and cannot become ground truth by editing its manifest; the analyst must create a distinct reviewed MOT import after completing all missing-object, identity, team, shirt, and box corrections.

The official Torchreid source remains a useful MIT-licensed implementation reference, and the author's Hugging Face OSNet repository currently labels its model repository MIT. That repository's re-identification weights identify MSMT17 as training data, while the official Torchreid dataset guide points to the external MSMT17 release. Those records do not by themselves establish product-use rights for the underlying person imagery or complete Football Science's separate identity-use review. No OSNet re-identification weight is therefore installed as a candidate yet. The new screen is ready for an exact native provider only after its source, model bytes, model licence, every training dataset, dataset terms, rights decision, and identity-use decision are pinned in the provider manifest.

Football role classification now has the same semantic protection. A `classify:role` request accepts at most eight metadata-only anchors, with at most four per role, binding one sealed generic-person trajectory to `player` or `referee`. The normalized anchors are sorted into the request fingerprint. A provider may always abstain with `unknown`, but it may emit a football role only when that role is represented by an analyst-bound anchor and it may never contradict an anchor on the same trajectory. Candidate review tracks retain only the sealed trajectory and association-artifact fingerprints needed for calibration. Relabeling an unknown trajectory or explicitly confirming an existing role creates a device-local audit record; rerun is allowed only when the new association artifact is byte-identical. A changed association result invalidates every old role and team anchor before classification or re-identification can run. The local anchors and source binding are removed from central metadata sync.

Team classification now has an explicit semantic anchor boundary. A classification request may carry at most eight metadata-only anchors, each binding `home` or `away` to one already sealed player trajectory, or to one sealed generic-person trajectory when the same provider also owns `classify:role`; it cannot reference a ball, referee, unknown trajectory, or the same trajectory twice. The normalized anchors are sorted and included in the stage-request fingerprint. A provider may always abstain with `unknown`, but it may emit `home` or `away` only for a `player` role when that side has an analyst-bound anchor, and it may not contradict the anchor assigned to the trajectory it is classifying. Referee and unknown roles cannot carry team or shirt fields. This keeps role and kit classification replaceable while preventing an arbitrary person class or cluster permutation from silently becoming football semantics.

The reproducible re-identification commands are:

```bash
npm --prefix desktop/local-video-app run tracking:candidate:screen:reidentification -- \
  --pack <annotation-pack.json> \
  --detection-screening <detection-screening-directory> \
  --association-screening <association-screening-directory> \
  --provider <provider-id>@<provider-version> \
  --output <new-reidentification-screening-directory>

npm --prefix desktop/local-video-app run tracking:candidate:screen:reidentification:verify -- \
  --pack <annotation-pack.json> \
  --detection-screening <detection-screening-directory> \
  --association-screening <association-screening-directory> \
  --screening <reidentification-screening-directory>
```

Operational readiness has two local checks. `fs-player:tracking:preflight` verifies the pinned install, full manifest, Football Science runtime hash, exact upstream execution tree, checkpoint, Python, PyTorch, device, and FFmpeg. `fs-player:tracking:smoke` additionally performs real model inference over generated synthetic video, validates propagation through the production artifact boundary, and removes all temporary media. The fixture uses a deterministic textured pitch and two internally striped targets; a flat-color fixture is not a valid propagation test because it gives a segmentation model no stable visual boundary after the prompt frame. With `--batch`, the smoke compares one shared-state two-object run against two repeated single-target jobs. With `--warm`, it runs the same target twice in one resident worker and separately reports startup, model load, first-job, and warm-job latency.

The installed `1.3.0` checks on 2026-08-26 are deliberately reported without hiding latency. The verified macOS profile uses eight CPU threads and an effective 6.25 sampled frames per second. Two consecutive strict process runs completed the identical warm one-second fixture in 7.394 and 7.501 seconds, a 7.394-7.501 real-time factor against the reference maximum of 1. The first post-install run loaded the resident worker in 2.064 seconds, including 1.358 seconds of model load, and completed its first job in 10.086 seconds. An immediate process restart benefited from OS caches and started faster, but did not improve warm inference. Across the final runs, sampling took 33-76 ms and forward propagation took 6.450-6.506 seconds. Forward propagation consistently consumed about 87% of end-to-end latency, so lowering media-quality or weakening output checks would not solve the measured bottleneck. Both runs produced the same 6 points, 80% observed coverage, and 0.966 mean confidence. A same-provider MPS comparison produced denser synthetic output, 13 points at 96% coverage and 0.994 confidence, but its 24.959-second warm job was more than three times slower than CPU. macOS therefore defaults to CPU while reviewed device and thread-count overrides remain available for comparison. Resident reuse, runtime identity, stage measurement, and inference wiring are proven; workstation-speed approval and real-match football quality are not. Only the representative real-match benchmark can establish football quality.

The 2026-08-31 branch verification preserves that fail-visible result. Provider preflight passed for the exact `1.3.0` CPU installation with eight threads, PyTorch `2.5.1`, no inference network, and the pinned checkpoint. The warm smoke returned the same 6 points, 80% observed coverage, and 0.966 mean confidence, but required 8.221 seconds for one second of video; `warmWithinReferenceBudget` and `realMatchQualityProven` therefore remained false. The native stage-runner probe separately passed the real macOS default-deny sandbox, proved network denial, emitted no stdout/stderr, and accepted only 29 bounded result bytes after input-identity revalidation. These are operational and isolation proofs, not substitute football-quality evidence.

## Benchmark Evidence Profiles

Segmentation/propagation and full-scene tracking use separate ground-truth contracts. `selected-player-pilot-v1` contains exactly one prompted, verified player and measures only the selected-object capability that SAM claims. `football-scene-pilot-v1` contains every visible player, ball, and referee, requires an exhaustive-scene attestation, and remains mandatory for detection, association, re-identification, and classification approval through TrackEval. One suite can contain only one profile, and its type becomes immutable when a locked case or raw provider run exists. This reduces unnecessary annotation for the installed SAM benchmark without weakening any future full-scene approval requirement.

## Execution Result Boundary

Every future pipeline stage returns `football-science-tracking-stage-result-v1` through one strict local validator. Serialized output is size-bounded and UTF-8 validated before parsing. The result is then bound to the exact provider runtime fingerprint, capability set, video-source fingerprint, requested time range, and a canonical SHA-256 fingerprint of the complete stage request before any value can reach FS Player. Detection binds the source/range request; segmentation binds geometry-only prompts; association binds complete ordered observations; and re-identification/classification bind complete trajectories containing those observations. Nested unknown fields, changed ordering, hidden media/identity data, or cross-provider inputs therefore invalidate the result instead of silently creating a mixed pipeline run.

- detection may return only bounded boxes, timestamps, entity class, and confidence; a generic `detect:person` provider cannot also claim that the same output distinguishes player or referee roles
- association may reference each known observation at most once and cannot introduce identity data
- re-identification may return only an opaque device-local identity key and confidence for known player trajectories; embeddings, images, paths, and player identities are rejected
- role classification may map only a known person/player trajectory to `player`, `referee`, or `unknown` with confidence; team and shirt fields are accepted only for the resolved player role, and no classification stage may assign a Football Science player identity
- segmentation reuses the existing bounded selected-object track validator

Candidate outputs can pass this structural boundary for benchmark evaluation, but normal activation uses the stricter activated boundary and fails closed unless the exact provider manifest, original report, raw-run evidence, and reproducible evidence artifact all pass readiness. Each candidate run is additionally sealed into `football-science-tracking-candidate-stage-run-v1`: an immutable raw-evidence artifact containing only canonical stage data, provider/build fingerprints, source/range checksums, result checksum, and bounded sandbox telemetry. The browser verifies the exact evidence response SHA-256 and same-origin route before accepting it.

Analyst review now treats structural corrections as reversible operations rather than edits to provider evidence. Two non-overlapping same-scope trajectory fragments can be merged only when their entity class, confirmed identity labels, temporal gap, and spatial boundary are compatible; every sample is preserved, the absorbed review track is archived, bound graphics migrate to the retained track, and undo/redo restores both tracks and bindings atomically. A false-positive trajectory can be rejected without deletion: its review copy is archived and excluded from overlays, spatial analysis, dynamic graphics, and ground truth while raw candidate evidence remains unchanged. `reject` and `restore` have distinct idempotent correction records, and the selected rejected row remains available for immediate restore. A detection that the association stage leaves unassigned is also preserved as its own automatic review track with zero player-identity confidence; it remains counted as an association miss, but it can no longer disappear before analyst review.

The benchmark-only full-scene orchestrator uses one of two coherent orders. Explicit football-role detectors run detection, association, re-identification, then classification. Generic-person detectors run detection, association, role/team classification, then pass only role-confirmed player trajectories to re-identification. It materializes association references from the exact detection observations, requires one source/range across all four stages, and hashes every stage evidence/result into one composite lineage. The composite player, ball, referee, and unresolved review tracks enter the existing professional workflow with confidence and discontinuity evidence; identity assignment, entity correction, box correction, visibility, split, continuity, and identity-swap operations affect review copies only. Raw candidate evidence stays unchanged so later quality reports can measure both model output and analyst correction burden. The immutable pipeline artifact is stored only inside the exact on-device organization/team/user/source/item scope and is revalidated on restore. Benchmark predictions are then split into four provider-run artifacts, and the analyst must select the exact stage provider under evaluation; this selection cannot activate that candidate for normal tracking.

Provider evidence also applies a non-overridable workstation policy of at most `1.0x` real time to every attested case. A benchmark profile may demand faster inference, but it cannot raise that ceiling. Each raw run binds the coarse execution profile that produced that time: device class, runtime mode, CPU threads, effective sample rate, model residency, and worker reuse. One approval suite must use one exact profile; mixed or incomplete profiles cannot be hidden behind an aggregate. Missing processing evidence, a weakened report threshold, or one slower case prevents provider approval even when all quality metrics pass.

## Raw Run Evidence Boundary

FS Player snapshots normalized provider output immediately after local inference and before persistence, merge, identity correction, continuity repair, or any other analyst edit. Every `football-science-tracking-provider-run-v1` artifact records the exact provider id, version, contract protocol, stage, capability set and shared execution fingerprint over upstream commit/source, model hashes and packaged runtime, plus source SHA-256, angle, frame, range, positive measured processing time, and automatic trajectory output. Corrected points or correction records are rejected. The execution fingerprint is deliberately separate from the broader reviewed-manifest fingerprint that also binds provenance, licence policy and runtime limits.

Provider runs remain separate from ground truth until the local assembler validates both suites and binds their SHA-256 hashes and exact used run ids into `football-science-tracking-provider-run-evidence-v1`. Provider approval carries those hashes and the run-id set hash into the reviewed manifest. A report made from another provider build, video, range, frame, or post-correction output therefore cannot approve the installed engine.

## Independent Reference Evaluator

Provider approval for detection, association, or re-identification requires a second report from the official TrackEval metric implementation. Football Science packages TrackEval as a local evaluator, not as a tracking provider:

- official source: https://github.com/JonathonLuiten/TrackEval
- pinned commit: `12c8791b303e0a0b50f753af204249e622d0281a`
- pinned source SHA-256: `435f0e6d865918332155f8104a98a04d50c2c3de5b985b96c8a71a0f5b62a0ac`
- licence: MIT
- metrics: HOTA, DetA, AssA, LocA, CLEAR/MOTA, and Identity/IDF1

The adapter sends normalized boxes, trajectory IDs, entity class, timestamps, and a source fingerprint to an isolated local process. It sends no video, image frames, file paths, URLs, analyst identity, player identity, team identity, or shirt numbers. The report is schema-validated, bounded, deterministic, and hashed before it can be referenced by a provider manifest.

The local evaluator preflight on 2026-08-31 reverified the exact pinned commit and source SHA-256 above with Python `3.12.13` and reproduced perfect synthetic control metrics before any Match 11 report was permitted. FS Player now derives a read-only Measurement Intelligence view only after a multi-object TrackEval report has passed its existing schema, report-hash, per-case and internal/reference cross-validation boundaries. It exposes all six approval metrics and their signed gate margins, the weakest measured entity and case, identity switches, fragmentations, and a dimension-specific next investigation. This view cannot calculate, alter or approve evidence; incomplete per-entity metrics, missing report identity or one failed case cross-check keep it visibly incomplete. Real Match 11 quality remains unmeasured until the five exhaustive human references are locked and evaluated.

## Candidate Stack

### Detection: YOLOX

YOLOX is the first detector candidate because its official implementation is a high-performance anchor-free detector under Apache-2.0 and supports several local deployment targets. Generic weights are not enough for elite football: the exact checkpoint and its training-data rights must be reviewed, pinned, fine-tuned where necessary, and passed through FS Player's real-match benchmark before approval.

- Source: https://github.com/Megvii-BaseDetection/YOLOX
- Licence: https://github.com/Megvii-BaseDetection/YOLOX/blob/main/LICENSE

### Association: ByteTrack

ByteTrack is the first association candidate because it accepts detections from another detector and explicitly retains low-score observations to recover occluded objects. Its official code is MIT-licensed. MOT benchmark results do not prove football-specific identity continuity, so FS Player requires its own occlusion, camera-motion, and correction benchmark.

- Source: https://github.com/FoundationVision/ByteTrack
- Licence: https://github.com/FoundationVision/ByteTrack/blob/main/LICENSE

### Re-identification: Torchreid OSNet

Torchreid is the first re-identification candidate because it supports image and video re-ID, cross-dataset evaluation, pretrained models, and OSNet-family architectures. Its code is MIT-licensed. Model weights and every training dataset have separate provenance, and no weight is approved until that chain and the football benchmark are complete.

- Source: https://github.com/KaiyangZhou/deep-person-reid
- Licence: https://github.com/KaiyangZhou/deep-person-reid/blob/master/LICENSE

### Segmentation: SAM 2.1

SAM 2.1 remains the selected-object segmentation and propagation stage. The official code and checkpoints are Apache-2.0. FS Player keeps its current pinned tiny checkpoint as the workstation baseline and can benchmark larger variants later against latency and memory budgets.

- Source: https://github.com/facebookresearch/sam2
- Licence: https://github.com/facebookresearch/sam2/blob/main/LICENSE

### Football reference: SoccerNet Game State

The official SoccerNet Game State baseline is a valuable research reference for person detection, role, team, shirt number, re-identification, pitch calibration, and game-state evaluation. It is not the complete FS Player candidate: the audited baseline does not provide the required ball stage, its source is GPL-3.0, its locked TrackLab dependency executes Ultralytics `8.3.123` under AGPL-3.0, PRTReID/BPBreID have conflicting repository and package licence declarations, and one newer KPR weight repository has no licence tag. It therefore remains an isolated benchmark reference. See `TRACKING_SOCCERNET_GSR_AUDIT.md` for exact revisions, wheel hashes, model records, and blockers.

## Post-screening Provider Order

The real-match screening fixes the next implementation order. It is a dependency order, not a claim that the named candidates are already fit for production.

1. **Detection baseline:** keep `yolox-s-coco-reference@0.1.2` installed only as a reproducible negative control. Do not spend full ten-minute annotation or approval effort on it while every measured case remains above `3.49x` real time. The next detector candidate must either independently pass player, ball, and referee coverage, or declare only generic person plus ball detection and pair with a separately approved football-role classifier.
2. **Association:** package and screen ByteTrack only after one detector candidate passes structural, isolation, and workstation screening. Association quality depends on the exact incoming detections; measuring it against a rejected detector would confound detector misses with continuity performance. Its output remains trajectory references only and cannot introduce team or player identity.
3. **Ball and referee:** retain a separate `detect:ball` gate in every path. Explicit-role detectors retain `detect:referee`; generic detectors instead require `classify:role` against exhaustive player/referee ground truth. A player-heavy aggregate can never hide a missing small ball or official role. Zero referee output from a generic COCO person class is not referee evidence.
4. **Player re-identification:** do not install OSNet weights until source, model, every training dataset, dataset rights, and identity-use review are pinned. Re-ID receives only normalized trajectories and emits opaque local identity keys; it cannot see Football Science player records or write player identity.
5. **Team and shirt classification:** team classification remains a later replaceable stage and can be approved independently of shirt number. Shirt-number capability requires its own model/data/identity-use evidence and must not block a safe team-only provider. Neither stage may classify ball or referee observations.
6. **Ground truth and correction:** candidate outputs remain immutable suggestion evidence. Only review copies enter identity assignment, box correction, split, visibility, continuity, identity-swap, undo, and redo. No screening status can mutate or lock independent ground truth; provider approval begins only after exhaustive reviewed reference evidence and independent TrackEval cross-validation exist.

This ordering prevents expensive downstream work from masking a detector that already fails the local performance contract, while preserving stage-level evidence so a better detector does not require replacing association, re-ID, or classification architecture.

## Deliberate Hold

The detector, association, re-ID, team, and shirt stages now have approval, runtime-integrity, resource-limit, approved and benchmark-only candidate registries, isolated execution, session-owned source reuse, and capability-scoped result boundaries, but no full-scene model is installed or activated. Choosing arbitrary public weights now would create false confidence and unresolved data/licence risk. The reference evaluator can be installed independently because it contains no tracking model and approves no provider by itself. The next provider approval point is a local real-match benchmark using representative Football Science footage. Only providers that improve measured quality without breaking workstation performance and whose report/evidence pair verifies exactly move from `candidate` to `approved-local-optional`.
