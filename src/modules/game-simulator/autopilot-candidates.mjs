import { createGameSimulatorAutopilotCandidateTargetHelpers } from "./autopilot-candidate-target-helpers.mjs";
import { createGameSimulatorAutopilotDribbleCandidates } from "./autopilot-dribble-candidates.mjs";
import { createGameSimulatorAutopilotFinalThirdCandidates } from "./autopilot-final-third-candidates.mjs";
import { createGameSimulatorAutopilotGoalkeeperDistributionCandidates } from "./autopilot-goalkeeper-distribution-candidates.mjs";
import { createGameSimulatorAutopilotLineBreakCandidates } from "./autopilot-line-break-candidates.mjs";
import { createGameSimulatorAutopilotPassCandidates } from "./autopilot-pass-candidates.mjs";
import { createGameSimulatorAutopilotRegainEscapeCandidates } from "./autopilot-regain-escape-candidates.mjs";
import { createGameSimulatorAutopilotRegainReleaseCandidates } from "./autopilot-regain-release-candidates.mjs";
import { createGameSimulatorAutopilotRestartCandidates } from "./autopilot-restart-candidates.mjs";
import { createGameSimulatorAutopilotShotCandidates } from "./autopilot-shot-candidates.mjs";
import { createGameSimulatorAutopilotSwitchLandingCandidates } from "./autopilot-switch-landing-candidates.mjs";

export function createGameSimulatorAutopilotCandidates(deps = {}) {
  const {
    angleBetween,
    chooseScoredCandidateWithVariation,
    chooseWideOverlapRunner,
    clamp,
    clampToPitch,
    computePassLaneClarity,
    computeTimeToCoverDistance,
    distance,
    getActionSpaceValue,
    getActionThreatGain,
    getAttackDirectionSign,
    getAttackingDepth,
    getAutoPilotCarryEndProductContext,
    getAutoPilotFlowContext,
    getAutoPilotRegainContext,
    getAutoPilotRoleStrength,
    getBreakawayCarryTarget,
    getCarryLaneOpenSpaceScore,
    getCarryRunwayProfile,
    getDepthPoint,
    getDepthX,
    getDistanceFromOwnGoal,
    getFootUsageScore,
    getForwardFacingSpaceTwoContext,
    getForwardProgressionWindow,
    getGoalMouthTarget,
    getGoalkeeperTargetOpenness,
    getHighValueAttackTarget,
    getKickoffSupportId,
    getNearestOpponentGapInCarryLane,
    getOffensiveAutopilotProfile,
    getOffensiveRoleKey,
    getOpenGrassCarryContext,
    getOpponentGoalCenter,
    getOpponentPenaltySpot,
    getOpponentPressureAtPoint,
    getPitchLaneIndex,
    getPitchLaneKey,
    getPitchThreatProfile,
    getPlayerBallControlPoint,
    getPlayerById,
    getPlayerMagnetLabel,
    getPlayerPressureLoad,
    getPlayerTendency,
    getPossessionRhythmContext,
    getRecentPossessionSteps,
    getRecordedStepDuration,
    getRunwayCarryTarget,
    getShotAngleQuality,
    getShotWindowProfile,
    getState,
    getSwitchLandingAttackTarget,
    getWideEntryPrincipleContext,
    getWideSideSign,
    isBylineZone,
    isGoalkeeper,
    isInsideOpponentBox,
    isInsideOwnBox,
    isLastStepKickoffResetForTeam,
    isPassReceiverOffside,
    isTransitionAttackStyle,
    isWideChannel,
    isWidePrincipleZone,
    kickoffOpeningProfiles,
    lerp,
    normalize,
    pitch,
    resolveBallActionProfile,
    resolveShotTarget,
    teams,
    uniquePrincipleLabels,
    win,
  } = deps;

const {
  getAutoPilotShotTarget,
  getAutoPilotBoxTarget,
  getCornerDeliveryTarget,
  chooseCornerDeliveryRunner,
  getFreeKickDeliveryTarget,
  chooseFreeKickShortReceiver,
} = createGameSimulatorAutopilotCandidateTargetHelpers({
  angleBetween,
  clamp,
  clampToPitch,
  computePassLaneClarity,
  computeTimeToCoverDistance,
  distance,
  getAttackDirectionSign,
  getAutoPilotRoleStrength,
  getFootUsageScore,
  getGoalMouthTarget,
  getOffensiveRoleKey,
  getOpponentGoalCenter,
  getOpponentPenaltySpot,
  getPlayerBallControlPoint,
  getPlayerPressureLoad,
  getShotWindowProfile,
  getState,
  getWideSideSign,
  isGoalkeeper,
  isWideChannel,
  pitch,
  resolveShotTarget,
  teams,
  win,
});

function getTeamSupportCountAroundPoint(teamId, point, excludedIds = new Set(), radius = 12) {
return state.players.reduce((count, player) => {
if (player.team !== teamId || excludedIds.has(player.id) || isGoalkeeper(player)) {
return count;
}
return count + (distance(player.position, point) <= radius ? 1 : 0);
}, 0);
}

const autoPilotGoalkeeperDistributionCandidates = createGameSimulatorAutopilotGoalkeeperDistributionCandidates({
  clamp,
  computePassLaneClarity,
  computeTimeToCoverDistance,
  distance,
  getAttackDirectionSign,
  getAutoPilotRoleStrength,
  getDepthPoint,
  getOffensiveRoleKey,
  getPlayerBallControlPoint,
  getPlayerMagnetLabel,
  getPlayerPressureLoad,
  getState,
  getTeamSupportCountAroundPoint,
  getWideSideSign,
  isGoalkeeper,
  pitch,
  resolveBallActionProfile,
  teams,
});
const {
  getGoalkeeperDistributionPressure,
  getGoalkeeperDirectReleaseTarget,
  buildAutoPilotGoalkeeperDistributionCandidate,
} = autoPilotGoalkeeperDistributionCandidates;

const autoPilotShotCandidates = createGameSimulatorAutopilotShotCandidates({
  clamp,
  distance,
  getAttackingDepth,
  getAutoPilotCarryEndProductContext,
  getAutoPilotRoleStrength,
  getAutoPilotShotTarget,
  getOpponentGoalCenter,
  getPlayerBallControlPoint,
  getPlayerPressureLoad,
  getRecentPossessionSteps,
  getShotWindowProfile,
  getState,
  isInsideOpponentBox,
  pitch,
});
const {
  buildAutoPilotShotCandidate,
} = autoPilotShotCandidates;

const autoPilotRestartCandidates = createGameSimulatorAutopilotRestartCandidates({
  chooseCornerDeliveryRunner,
  chooseFreeKickShortReceiver,
  chooseScoredCandidateWithVariation,
  clamp,
  clampToPitch,
  computePassLaneClarity,
  distance,
  getAttackDirectionSign,
  getAttackingDepth,
  getAutoPilotRoleStrength,
  getAutoPilotShotTarget,
  getCornerDeliveryTarget,
  getFreeKickDeliveryTarget,
  getGoalkeeperTargetOpenness,
  getKickoffSupportId,
  getOffensiveRoleKey,
  getOpponentGoalCenter,
  getPitchLaneIndex,
  getPitchLaneKey,
  getPitchThreatProfile,
  getPlayerBallControlPoint,
  getPlayerById,
  getPlayerMagnetLabel,
  getPlayerPressureLoad,
  getShotAngleQuality,
  getShotWindowProfile,
  getState,
  getTeamSupportCountAroundPoint,
  getWideSideSign,
  isGoalkeeper,
  isLastStepKickoffResetForTeam,
  kickoffOpeningProfiles,
  pitch,
  resolveBallActionProfile,
  teams,
});
const {
  buildAutoPilotKickoffCandidate,
  getLastKickoffOpeningProfile,
  getKickoffOpeningCandidateFit,
  buildAutoPilotPostKickoffResetCandidate,
  buildAutoPilotCornerCandidate,
  buildAutoPilotThrowInCandidate,
  buildAutoPilotPenaltyCandidate,
  buildAutoPilotFreeKickCandidate,
} = autoPilotRestartCandidates;
const autoPilotRegainReleaseCandidates = createGameSimulatorAutopilotRegainReleaseCandidates({
  clamp,
  computePassLaneClarity,
  computeTimeToCoverDistance,
  distance,
  getAttackDirectionSign,
  getAutoPilotRegainContext,
  getAutoPilotRoleStrength,
  getHighValueAttackTarget,
  getOffensiveRoleKey,
  getPitchThreatProfile,
  getPlayerBallControlPoint,
  getPlayerMagnetLabel,
  getPlayerPressureLoad,
  getState,
  getTeamSupportCountAroundPoint,
  getWideSideSign,
  isGoalkeeper,
  isPassReceiverOffside,
  resolveBallActionProfile,
  teams,
});
const {
  buildAutoPilotRegainReleaseCandidate,
} = autoPilotRegainReleaseCandidates;

const autoPilotRegainEscapeCandidates = createGameSimulatorAutopilotRegainEscapeCandidates({
  chooseScoredCandidateWithVariation,
  clamp,
  clampToPitch,
  computePassLaneClarity,
  distance,
  getAttackDirectionSign,
  getAutoPilotRegainContext,
  getAutoPilotRoleStrength,
  getCarryLaneOpenSpaceScore,
  getDistanceFromOwnGoal,
  getNearestOpponentGapInCarryLane,
  getOffensiveRoleKey,
  getOpponentPressureAtPoint,
  getPlayerBallControlPoint,
  getPlayerMagnetLabel,
  getPlayerPressureLoad,
  getState,
  getTeamSupportCountAroundPoint,
  getWideSideSign,
  isGoalkeeper,
  isInsideOwnBox,
  isTransitionAttackStyle,
  normalize,
  pitch,
  teams,
});
const {
  getPressedRegainExitVector,
  buildAutoPilotPressedRegainExitCandidate,
  buildAutoPilotDangerZoneEscapeCandidate,
} = autoPilotRegainEscapeCandidates;

const autoPilotFinalThirdCandidates = createGameSimulatorAutopilotFinalThirdCandidates({
  chooseWideOverlapRunner,
  clamp,
  clampToPitch,
  computePassLaneClarity,
  computeTimeToCoverDistance,
  distance,
  getActionSpaceValue,
  getAttackDirectionSign,
  getAttackingDepth,
  getAutoPilotBoxTarget,
  getAutoPilotRoleStrength,
  getHighValueAttackTarget,
  getOffensiveRoleKey,
  getPlayerBallControlPoint,
  getPlayerMagnetLabel,
  getPlayerPressureLoad,
  getPlayerTendency,
  getState,
  getTeamSupportCountAroundPoint,
  getWideSideSign,
  isBylineZone,
  isGoalkeeper,
  isPassReceiverOffside,
  isWideChannel,
  isWidePrincipleZone,
  lerp,
  pitch,
  resolveBallActionProfile,
  teams,
});
const {
  buildAutoPilotBoxDeliveryCandidate,
  getFinalThirdCombinationVariants,
  buildAutoPilotFinalThirdCombinationCandidate,
  buildAutoPilotWideOverlapCandidate,
} = autoPilotFinalThirdCandidates;
const autoPilotSwitchLandingCandidates = createGameSimulatorAutopilotSwitchLandingCandidates({
  buildAutoPilotBoxDeliveryCandidate,
  chooseWideOverlapRunner,
  clamp,
  computePassLaneClarity,
  distance,
  getActionSpaceValue,
  getAttackDirectionSign,
  getAttackingDepth,
  getAutoPilotRoleStrength,
  getOffensiveRoleKey,
  getPitchLaneIndex,
  getPitchThreatProfile,
  getPlayerBallControlPoint,
  getPlayerMagnetLabel,
  getPlayerPressureLoad,
  getPlayerTendency,
  getRecentPossessionSteps,
  getRecordedStepDuration,
  getState,
  getSwitchLandingAttackTarget,
  getTeamSupportCountAroundPoint,
  getWideSideSign,
  isGoalkeeper,
  isPassReceiverOffside,
  isWidePrincipleZone,
  teams,
  uniquePrincipleLabels,
});
const {
  getLastSwitchLandingActionContext,
  buildAutoPilotSwitchLandingContinuationCandidate,
} = autoPilotSwitchLandingCandidates;
const autoPilotLineBreakCandidates = createGameSimulatorAutopilotLineBreakCandidates({
  clamp,
  clampToPitch,
  computePassLaneClarity,
  computeTimeToCoverDistance,
  distance,
  getActionSpaceValue,
  getAttackDirectionSign,
  getAttackingDepth,
  getAutoPilotRoleStrength,
  getDepthX,
  getForwardProgressionWindow,
  getHighValueAttackTarget,
  getOffensiveRoleKey,
  getPitchThreatProfile,
  getPlayerMagnetLabel,
  getPlayerPressureLoad,
  getPlayerTendency,
  getState,
  getTeamSupportCountAroundPoint,
  getWideSideSign,
  isGoalkeeper,
  isPassReceiverOffside,
  lerp,
  pitch,
  resolveBallActionProfile,
  teams,
});
const {
  buildAutoPilotThroughBallCandidate,
  buildAutoPilotBetweenLinesCandidate,
} = autoPilotLineBreakCandidates;
const autoPilotPassCandidates = createGameSimulatorAutopilotPassCandidates({
  clamp,
  computePassLaneClarity,
  distance,
  getActionSpaceValue,
  getActionThreatGain,
  getAttackDirectionSign,
  getAttackingDepth,
  getAutoPilotRoleStrength,
  getForwardFacingSpaceTwoContext,
  getOffensiveRoleKey,
  getPitchLaneKey,
  getPitchThreatProfile,
  getPlayerBallControlPoint,
  getPlayerMagnetLabel,
  getPlayerPressureLoad,
  getPlayerTendency,
  getPossessionRhythmContext,
  getState,
  getTeamSupportCountAroundPoint,
  getWideEntryPrincipleContext,
  isPassReceiverOffside,
  isWideChannel,
  pitch,
  teams,
});
const {
  buildAutoPilotPassCandidates,
} = autoPilotPassCandidates;
const autoPilotDribbleCandidates = createGameSimulatorAutopilotDribbleCandidates({
  clamp,
  clampToPitch,
  distance,
  getActionSpaceValue,
  getActionThreatGain,
  getAttackDirectionSign,
  getAutoPilotFlowContext,
  getAutoPilotRoleStrength,
  getBreakawayCarryTarget,
  getCarryLaneOpenSpaceScore,
  getCarryRunwayProfile,
  getAttackingDepth,
  getForwardFacingSpaceTwoContext,
  getForwardProgressionWindow,
  getNearestOpponentGapInCarryLane,
  getOffensiveAutopilotProfile,
  getOffensiveRoleKey,
  getOpenGrassCarryContext,
  getOpponentGoalCenter,
  getPitchThreatProfile,
  getPlayerBallControlPoint,
  getPlayerPressureLoad,
  getPlayerTendency,
  getRunwayCarryTarget,
  isWideChannel,
  lerp,
  pitch,
  teams,
});
const {
  getAutoPilotDribbleTarget,
  buildAutoPilotDribbleCandidate,
} = autoPilotDribbleCandidates;

  return {
    getAutoPilotShotTarget,
    getAutoPilotBoxTarget,
    getCornerDeliveryTarget,
    chooseCornerDeliveryRunner,
    getFreeKickDeliveryTarget,
    chooseFreeKickShortReceiver,
    getAutoPilotDribbleTarget,
    getTeamSupportCountAroundPoint,
    getGoalkeeperDistributionPressure,
    getGoalkeeperDirectReleaseTarget,
    buildAutoPilotGoalkeeperDistributionCandidate,
    buildAutoPilotShotCandidate,
    buildAutoPilotKickoffCandidate,
    getLastKickoffOpeningProfile,
    getKickoffOpeningCandidateFit,
    buildAutoPilotPostKickoffResetCandidate,
    buildAutoPilotCornerCandidate,
    buildAutoPilotThrowInCandidate,
    buildAutoPilotPenaltyCandidate,
    buildAutoPilotFreeKickCandidate,
    buildAutoPilotRegainReleaseCandidate,
    getPressedRegainExitVector,
    buildAutoPilotPressedRegainExitCandidate,
    buildAutoPilotDangerZoneEscapeCandidate,
    buildAutoPilotBoxDeliveryCandidate,
    getFinalThirdCombinationVariants,
    buildAutoPilotFinalThirdCombinationCandidate,
    buildAutoPilotWideOverlapCandidate,
    getLastSwitchLandingActionContext,
    buildAutoPilotSwitchLandingContinuationCandidate,
    buildAutoPilotThroughBallCandidate,
    buildAutoPilotBetweenLinesCandidate,
    buildAutoPilotPassCandidates,
    buildAutoPilotDribbleCandidate,
  };
}
