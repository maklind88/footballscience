# File Access

Owns native file selection, permission prompts, local file handles, and device-only video availability checks.
It must never sync raw video bytes or absolute local paths to Football Science metadata APIs.
