export const platformRoles = Object.freeze(["admin", "coach", "analyst", "performance", "medical", "guest"]);

export const protectedStorageKeys = Object.freeze([
  "football-workspace-hub-v3",
  "football-periodization-v2",
  "football-schedule-v1",
  "football-session-planner-v3",
  "football-session-exercise-library-v1",
  "football-session-exercise-library-backup-v1",
  "football-dashboard-tasks-v1",
  "football-dashboard-chat-v1",
  "football-dashboard-notification-seen-v1",
  "football-dashboard-tutorial-prefs-v1",
  "football-dashboard-news-seen-v1",
  "football-medical-team-v1",
  "football-player-profiles-v1",
  "football-simulator-sequence-v1",
  "football-simulator-sequence-library-v2",
]);

export const platformModuleMigrationOrder = Object.freeze([
  "home",
  "chat",
  "schedule",
  "exercise-library",
  "session-planner",
  "periodization",
  "medical-team",
  "game-simulator",
]);

export const platformModules = Object.freeze([
  Object.freeze({
    id: "platform-shell",
    label: "Platform Shell",
    stage: "core",
    storageKeys: Object.freeze(["football-workspace-hub-v3"]),
    futureTables: Object.freeze([]),
    viewRoles: Object.freeze(platformRoles),
    editRoles: Object.freeze(["admin"]),
    emits: Object.freeze(["workspace.opened", "profile.opened", "auth.signed-out"]),
    consumes: Object.freeze(["data-safety.changed"]),
  }),
  Object.freeze({
    id: "home",
    label: "Home",
    stage: "module",
    storageKeys: Object.freeze([
      "football-dashboard-tasks-v1",
      "football-dashboard-notification-seen-v1",
      "football-dashboard-tutorial-prefs-v1",
      "football-dashboard-news-seen-v1",
    ]),
    futureTables: Object.freeze(["tasks"]),
    viewRoles: Object.freeze(["admin", "coach", "analyst", "performance", "medical", "guest"]),
    editRoles: Object.freeze(["admin", "coach", "analyst", "performance", "medical"]),
    emits: Object.freeze(["task.created", "task.completed"]),
    consumes: Object.freeze(["chat.unread-count.changed"]),
  }),
  Object.freeze({
    id: "chat",
    label: "Team Chat",
    stage: "module",
    storageKeys: Object.freeze(["football-dashboard-chat-v1"]),
    futureTables: Object.freeze(["chat_threads", "chat_messages", "chat_read_receipts", "chat_reactions"]),
    viewRoles: Object.freeze(["admin", "coach", "analyst", "performance", "medical", "guest"]),
    editRoles: Object.freeze(["admin", "coach", "analyst", "performance", "medical"]),
    emits: Object.freeze(["chat.message-sent", "chat.message-read"]),
    consumes: Object.freeze(["profile.updated"]),
  }),
  Object.freeze({
    id: "schedule",
    label: "Schedule",
    stage: "module",
    storageKeys: Object.freeze(["football-schedule-v1"]),
    futureTables: Object.freeze(["schedule_events"]),
    viewRoles: Object.freeze(["admin", "coach", "analyst", "performance", "medical", "guest"]),
    editRoles: Object.freeze(["admin", "coach"]),
    emits: Object.freeze(["schedule.event-created", "schedule.event-updated", "schedule.date-selected"]),
    consumes: Object.freeze([]),
  }),
  Object.freeze({
    id: "exercise-library",
    label: "Exercise Library",
    stage: "module",
    storageKeys: Object.freeze([
      "football-session-exercise-library-v1",
      "football-session-exercise-library-backup-v1",
    ]),
    futureTables: Object.freeze(["exercises", "exercise_versions"]),
    viewRoles: Object.freeze(["admin", "coach", "analyst", "performance", "medical"]),
    editRoles: Object.freeze(["admin", "coach"]),
    emits: Object.freeze(["exercise.saved", "exercise.archived", "exercise.restored"]),
    consumes: Object.freeze(["session.block-updated"]),
  }),
  Object.freeze({
    id: "session-planner",
    label: "Session Planner",
    stage: "module",
    storageKeys: Object.freeze(["football-session-planner-v3"]),
    futureTables: Object.freeze(["sessions", "session_blocks"]),
    viewRoles: Object.freeze(["admin", "coach", "analyst", "performance", "medical"]),
    editRoles: Object.freeze(["admin", "coach"]),
    emits: Object.freeze(["session.block-updated", "session.date-selected"]),
    consumes: Object.freeze(["schedule.date-selected", "periodization.day-updated", "medical.availability-updated"]),
  }),
  Object.freeze({
    id: "periodization",
    label: "Periodization",
    stage: "module",
    storageKeys: Object.freeze(["football-periodization-v2"]),
    futureTables: Object.freeze(["periodization_days"]),
    viewRoles: Object.freeze(["admin", "coach", "analyst", "performance", "medical"]),
    editRoles: Object.freeze(["admin", "coach", "performance"]),
    emits: Object.freeze(["periodization.day-updated"]),
    consumes: Object.freeze(["schedule.event-updated"]),
  }),
  Object.freeze({
    id: "medical-team",
    label: "Medical Team",
    stage: "module",
    storageKeys: Object.freeze(["football-medical-team-v1"]),
    futureTables: Object.freeze(["medical_availability", "medical_injury_plans"]),
    viewRoles: Object.freeze(["admin", "coach", "performance", "medical"]),
    editRoles: Object.freeze(["admin", "medical", "performance"]),
    emits: Object.freeze(["medical.availability-updated"]),
    consumes: Object.freeze(["profile.updated"]),
  }),
  Object.freeze({
    id: "player-profiles",
    label: "Player Profiles",
    stage: "module",
    storageKeys: Object.freeze(["football-player-profiles-v1"]),
    futureTables: Object.freeze(["profiles", "player_profiles", "memberships"]),
    viewRoles: Object.freeze(["admin", "coach", "performance", "medical"]),
    editRoles: Object.freeze(["admin", "coach"]),
    emits: Object.freeze(["profile.updated"]),
    consumes: Object.freeze(["medical.availability-updated"]),
  }),
  Object.freeze({
    id: "game-simulator",
    label: "Game Simulator",
    stage: "module",
    storageKeys: Object.freeze(["football-simulator-sequence-v1", "football-simulator-sequence-library-v2"]),
    futureTables: Object.freeze(["simulator_sequences", "simulator_sequence_versions"]),
    viewRoles: Object.freeze(["admin", "coach", "analyst", "performance"]),
    editRoles: Object.freeze(["admin", "coach", "analyst"]),
    emits: Object.freeze(["simulator.sequence-saved", "simulator.sequence-loaded"]),
    consumes: Object.freeze([]),
  }),
]);

export const platformModuleIds = Object.freeze(platformModules.map((module) => module.id));

export function getProtectedStorageKeySet() {
  return new Set(protectedStorageKeys);
}

