# Football Science Project Memory

This folder is the lightweight memory system for Football Science. It exists so future Codex threads can start quickly without needing the whole chat history.

## Read Order For Future Work

1. Read `docs/AI_HANDOFF.md` first.
2. Read `docs/NEXT_STEPS.md` for the active backlog.
3. Read `docs/MODULES.md` before changing a module.
4. Read `docs/QA_CHECKLIST.md` before saying work is done.
5. Read `docs/DEPLOYMENT.md` before deploying to `footballscience.xyz`.

## Starting New Chats

Use `docs/CHAT_STARTERS.md` and the files in `docs/module-chats/`. Each file is a ready-made prompt for one focused module chat.

## Update Rule

After meaningful product changes, update the smallest relevant doc. Keep these files compact, practical, and current. The goal is not a perfect archive. The goal is fast continuation.

## Current Workspace

- Local path: `/Users/maklind/Documents/New project`
- Main app files: `index.html`, `app.js`, `styles.css`
- Live domain: `footballscience.xyz`
- Current app style: clean Apple-like coaching platform, premium, quiet, no filler explanations.
