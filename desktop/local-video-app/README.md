# Local Video App

Desktop bridge placeholder for future native file access.

MVP browser flow uses a local `<input type="file">` object URL and stores only metadata through `/api/video-analysis`.
This directory is reserved for the later companion app that can safely expose local file handles without sending video files to Supabase.
